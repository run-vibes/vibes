//! Configuration types for Iggy event log.

/// Configuration for the Iggy event log.
#[derive(Debug, Clone)]
pub struct IggyConfig {
    // Will be populated in subsequent tasks
    _placeholder: (),
}
